@using Proyecto_vuelos.Entidades
@model Pasajero
<div class="row">
    <div class="col-lg-12">
        <div class="panel panel-default">
            <div class="panel-heading">
                Registro
            </div>
            <div class="panel-body">

                <!--Formulario aquí-->
                <form action="@Url.Action("Guardar","Pasajerp")">
                    <input type="hidden" class="form-control" name="id" value="@Model.Id">
                    <div class="form-group">
                        <label>Nombre del pasajero:</label>
                        <input class="form-control" name="nombre" value="@if (Model.Id != 0) { @Model.Nombre}" />
                    </div>
                    <div class="form-group">
                        <label>Apellido Paterno del Pasajero:</label>
                        <input class="form-control" name="apellidoPaterno" value="@if (Model.Id != 0) { @Model.ApellidoPaterno}" />
                    </div>
                    <div class="form-group">
                        <label>Apellido Materno del Pasajero:</label>
                        <input class="form-control" name="apellidoPaterno" value="@if (Model.Id != 0) { @Model.ApellidoMaterno}" />
                    </div>
                    <div class="form-group">
                        <label>Fecha de Nacimiento del Pasajero:</label>
                        <input class="form-control" name="fechaNacimiento" value="@if (Model.Id != 0) { @Model.FechaNacimiento}" />
                    </div>

                    <button class="btn btn-success" type="submit">Guardar</button>

                </form>
            </div>
        </div>
    </div>
</div>


